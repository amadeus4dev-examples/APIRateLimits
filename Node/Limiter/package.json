{
  "name": "amadeus-rate-limiter",
  "version": "1.0.0",
  "description": "Rate limiter for using with the Amadeus Node SDK. Allows for requests management to avoid unexpected costs or rate limit errors.",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "build": "tsc"
  },
  "author": "Gustavo Bertoldi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.7.14",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "bottleneck": "^2.19.5"
  }
}
